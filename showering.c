#include<stdio.h>
int main()
{
    long long t;
    scanf("%d",&t);
    while(t--)
    {
        long long n,s,m;
        scanf("%lld%lld%lld",&n,&s,&m);
        long long l,r;
        for(int i=0;i<n;i++)
        {

            scanf("%lld%lld",&l,&r);
        }
        while(i<=m)
        {

        }
    }
}
